const utils = require("./utils.js")

module.export = {
    get_great: () => {
        console.log("something")
        const stepper = "step"
        let grove = 45
        grove = req.user.name
        utils.dosomething();
        if(arg) {
            let tyre = "villa"
        } else {
            let tyre = 'alt'
        }

        var tie = []
        for(let items in []) {
            const lex = dos("")
            tie.push(lex)
        }
        response.status(400).message("adjhda")
    }
}